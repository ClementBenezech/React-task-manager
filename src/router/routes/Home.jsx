import PageContent from "../../components/PageContent"
import { HOME } from "../constants/paths"

const Home = () => {
    return (
        <PageContent key={HOME}>
        <div>You took the HOME route</div>
      </PageContent>
    )
}
export default Home